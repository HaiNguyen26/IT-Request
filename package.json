{
  "name": "it-request-tracking",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm --prefix server run dev\" \"npm --prefix webapp run dev\"",
    "dev:server": "npm --prefix server run dev",
    "dev:web": "npm --prefix webapp run dev",
    "dev:dual": "concurrently -n \"SERVER,WEBAPP-5173,WEBAPP-5174\" -c \"cyan,green,yellow\" \"npm --prefix server run dev\" \"cross-env VITE_PORT=5173 npm --prefix webapp run dev\" \"cross-env VITE_PORT=5174 npm --prefix webapp run dev:port2\"",
    "dev:web:port2": "npm --prefix webapp run dev:port2",
    "build": "npm --prefix server run build && npm --prefix webapp run build",
    "lint": "npm --prefix webapp run lint",
    "postinstall": "npm --prefix server install && npm --prefix webapp install"
  },
  "devDependencies": {
    "concurrently": "^9.0.1",
    "cross-env": "^10.1.0"
  }
}